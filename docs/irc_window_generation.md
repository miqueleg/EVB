# IRC and window generation

Frame mapping is generic. Full-system coordinates can be supplied under `frames`, or IRC frame labels can be supplied under `irc.frame_labels`. If an IRC XYZ contains only reactive atoms, it must not be treated as a full-system coordinate source; provide embedded/relaxed full-system frames before calibration or sampling.
