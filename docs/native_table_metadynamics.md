# Native table metadynamics

Native table metadynamics is well-tempered metadynamics where the gap bias is stored in an OpenMM tabulated function inside the EVB force. The FEL reconstruction uses the WT-MetaD relation `F(s) = -gamma/(gamma-1) V_bias(s) + constant`.
