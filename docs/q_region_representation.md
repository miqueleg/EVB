# Q-region representation

Q-region EVB evaluates common environment terms once and mixes only Q-region residual energies. For PME systems with differing endpoint nonbonded parameters, exact local PME decomposition is not claimed. Use `shared_nonbonded_model` for a modified exact Hamiltonian or `local_pme_approx` for an explicitly approximate local correction.
